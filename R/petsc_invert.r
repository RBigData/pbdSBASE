#petsc_invert <- function(dim, ldim, Data, row_ptr, col_ind)
#{
#  storage.mode(dim) <- "integer"
#  storage.mode(ldim) <- "integer"
#  storage.mode(Data) <- "double"
#  storage.mode(row_ptr) <- "integer"
#  storage.mode(col_ind) <- "integer"
#  
#  out <- .Call(sbase_petsc_matinvert, dim, ldim, Data, row_ptr, col_ind)
#  
#  return( out )
#}

